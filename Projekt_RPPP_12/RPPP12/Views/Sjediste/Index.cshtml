@model IEnumerable<RPPP12.Models.Sjediste>
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        <a asp-action="Create">Create New</a>
    </p>
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.ImeSjedista)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Adresa)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.ImeSjedista)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Adresa)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.SifraSjedista">Edit</a> |
                        <a asp-action="Details" asp-route-id="@item.SifraSjedista">Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.SifraSjedista">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</body>
</html>
